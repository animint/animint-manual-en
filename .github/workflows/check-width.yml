on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

name: Check width

jobs:
  check:
    steps:
      - uses: actions/checkout@v3
      - uses: r-lib/actions/setup-r@v2
      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          packages: animint2
      - name: Render book
        run: R --vanilla < test-width.R
